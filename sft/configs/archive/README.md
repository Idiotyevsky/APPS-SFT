# Archived SFT configurations

These configurations are retained for reproducibility and diagnosis only.

- 7b_protocol/: historical 7B protocol/SFT runs
- structural_probe/: module and structural ablations
- export/: historical export/merge configurations; do not use for the
  current dynamic-LoRA evaluation path
